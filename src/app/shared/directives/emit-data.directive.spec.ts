import { EmitDataDirective } from './emit-data.directive';

describe('EmitDataDirective', () => {
  it('should create an instance', () => {
    const directive = new EmitDataDirective();
    expect(directive).toBeTruthy();
  });
});
